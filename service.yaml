apiVersion: v1
kind: Service
metadata:
  name: feedback-app-service
  labels:
    app: feedback-app
spec:
  selector:
    app: feedback-app
  type: NodePort
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      nodePort: 30080
